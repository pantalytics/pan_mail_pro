<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <!--
        Four panes: where you are, what is waiting, the conversation, the
        record. Content carries the screen; the chrome stays out of the way.
        Progressive disclosure: the mailbox list holds the mailbox and its folders and
        nothing else, our own states filter the list one pane over, the
        routing diagnostic stays folded until somebody asks, and everything
        else is one click deeper on the record itself.

        Every pane is a region with a name, and everything clickable is a
        button. A div with a click handler is invisible to a keyboard and to a
        screen reader, and this screen is somebody's whole working day. The
        dividers are controls too: a separator you can drag, arrow-key, and
        fold with Enter.
    -->
    <t t-name="pan_mail_pro.ConversationView">
        <div class="o_content o_mailpro_inbox d-flex flex-column h-100"
             t-att-class="{ 'o_mailpro_small': panes.state.small, 'o_mailpro_narrow': panes.state.narrow }">

            <!--
                The bar every mail client has, in the order they all put it:
                the one thing you start from on the left, the one thing you
                look for with in the middle. No title: the control panel above
                already says Mail Pro / Inbox, and saying it twice is chrome
                competing with content.

                Writing a mail from here needs a record to write it on. This
                module files mail on documents, and a mail filed on nothing is
                the state the Inbox has a filter for. So New Email opens the
                same dialog linking opens, which asks the same two questions
                in the same two searchable steps, and once both are answered
                the composer opens in the conversation pane, the same one a
                reply uses.
            -->
            <div class="o_mailpro_topbar d-flex align-items-center gap-2">
                <div class="o_mailpro_topbar_start d-flex align-items-center gap-2">
                    <!--
                        Which panes are showing: the mailboxes, the list, the
                        record, in the order they sit. Pressed is showing.
                        They live here rather than on the dividers, where
                        they used to float over the conversation's own
                        header: a row of buttons over a header that already
                        carries a title, a chip and a tab strip is a second
                        menu bar stacked on the first. Top left is also where
                        every mail client has kept the mailboxes since there
                        were mail clients. The divider stays what it looks
                        like: a width to drag.
                    -->
                    <div class="o_mailpro_pane_toggles d-flex align-items-center flex-shrink-0">
                        <button t-foreach="panes.toggles()" t-as="pane" t-key="pane"
                                type="button"
                                t-attf-class="o_mailpro_pane_toggle o_mailpro_pane_toggle_{{pane}} {{ panes.folded(pane) ? '' : 'o_mailpro_pane_toggle_on' }}"
                                t-att-aria-pressed="panes.folded(pane) ? 'false' : 'true'"
                                t-att-aria-label="panes.toggleLabel(pane)"
                                t-att-title="panes.toggleLabel(pane)"
                                t-on-click="() => panes.togglePane(pane)">
                            <i class="fa" t-att-class="panes.paneIcon(pane)" aria-hidden="true"/>
                        </button>
                    </div>
                    <button type="button" class="btn btn-primary btn-sm o_mailpro_new"
                            t-on-click="() => this.newEmail()">
                        <i class="fa fa-pencil-square-o me-1" aria-hidden="true"/>New Email
                    </button>
                </div>
                <div class="o_mailpro_search_zone flex-grow-1 d-flex justify-content-center">
                    <input type="search" id="o_mailpro_search"
                           class="o_mailpro_search form-control form-control-sm"
                           aria-label="Search people and subjects"
                           placeholder="Search mail"
                           t-att-value="state.search"
                           t-on-input="(ev) => this.onSearchInput(ev)"
                           t-on-keydown="(ev) => this.onSearchKey(ev)"/>
                </div>
                <!-- The search stays in the middle of the window, which it
                     only does if both sides weigh the same. -->
                <div class="o_mailpro_topbar_end" aria-hidden="true"/>
            </div>

            <div t-if="state.error" class="o_mailpro_error d-flex align-items-center gap-2" role="alert">
                <span t-esc="state.error"/>
                <button class="btn btn-sm btn-link p-0" t-on-click="() => this.refresh()">Try again</button>
            </div>

            <div class="o_mailpro_panes d-flex flex-grow-1"
                 t-att-class="{ 'o_mailpro_zoomed': panes.state.zoom }">

                <!--
                    1. where you are: the mailbox, and inside it the folder.
                    The same shape as every mail client, because reading mail
                    is the one job people already know the layout of. Every
                    mailbox folds away the way Outlook folds an account: the
                    chevron opens and closes it, the name opens the mailbox
                    itself. A folded mailbox is not counted, so a mailbox list of six
                    accounts costs what the ones you actually watch cost.
                -->
                <!--
                    A folded pane stays in the DOM at no width, inert, so the
                    fold is a transition rather than a pane that blinks out.
                    The backdrop is the drawer's, on a phone, and fades with it.
                -->
                <div t-if="showMailboxList and panes.state.small" class="o_mailpro_mailbox_list_backdrop"
                     t-att-class="{ 'o_mailpro_folded': panes.folded('mailbox_list') }"
                     t-on-click="() => panes.closeMailboxList()"/>
                <nav t-if="showMailboxList" class="o_mailpro_mailbox_list"
                     t-att-class="{ 'o_mailpro_folded': panes.folded('mailbox_list') }"
                     t-att-inert="panes.folded('mailbox_list')"
                     aria-label="Mailboxes and folders"
                     t-attf-style="flex-basis: {{panes.state.mailbox_list}}px">
                    <t t-foreach="state.mailboxes" t-as="mailbox" t-key="mailbox.id">
                        <div class="o_mailpro_mailbox_row d-flex align-items-center">
                            <button type="button" class="o_mailpro_mailbox_toggle flex-shrink-0"
                                    t-att-aria-expanded="isExpanded(mailbox.id) ? 'true' : 'false'"
                                    t-attf-aria-label="{{ isExpanded(mailbox.id) ? 'Hide folders of' : 'Show folders of' }} {{ mailbox.email }}"
                                    t-on-click="() => this.toggleMailbox(mailbox.id)">
                                <i class="fa" aria-hidden="true"
                                   t-att-class="isExpanded(mailbox.id) ? 'fa-caret-down' : 'fa-caret-right'"/>
                            </button>
                            <button type="button"
                                    class="o_mailpro_mailbox d-flex align-items-center gap-2"
                                    t-att-class="{ 'o_mailpro_mailbox_active': mailbox.id === state.mailboxId }"
                                    t-att-aria-current="mailbox.id === state.mailboxId ? 'true' : undefined"
                                    t-on-click="() => this.setMailbox(mailbox.id)">
                                <span class="o_mailpro_mailbox_name text-truncate" t-esc="mailbox.email"/>
                            </button>
                        </div>
                        <div t-if="isExpanded(mailbox.id)" class="o_mailpro_folders">
                            <t t-set="folderMailbox" t-value="mailbox.id"/>
                            <t t-call="pan_mail_pro.ConversationFolders"/>
                        </div>
                    </t>
                    <!-- No mailbox configured yet: the folders are still the
                         reader's own mail, so the mailbox list is not empty. -->
                    <div t-if="!state.mailboxes.length" class="o_mailpro_folders">
                        <t t-set="folderMailbox" t-value="false"/>
                        <t t-call="pan_mail_pro.ConversationFolders"/>
                    </div>
                </nav>

                <t t-set="divider" t-value="'mailbox_list'"/>
                <t t-call="pan_mail_pro.PaneSplitter"/>

                <!-- 2. what is waiting -->
                <div t-if="showConversationList" class="o_mailpro_conversation_list" role="region" aria-label="Conversations"
                     t-att-class="{ 'o_mailpro_folded': panes.folded('conversation_list') }"
                     t-att-inert="panes.folded('conversation_list')"
                     t-attf-style="flex-basis: {{panes.state.conversation_list}}px">

                    <!--
                        The list header, where a mail client puts it: what you
                        are reading on the left, the filter on the right.

                        The states that are ours live in that menu rather than
                        in the mailbox list. A folder is a place mail is and the
                        provider decides what those are; "linked to nothing"
                        is a question we ask about it. One at a time, a second
                        click clears it, and the active one is named on the
                        button: a filter you cannot see from the closed menu
                        is a list that looks short for no reason.
                    -->
                    <div class="o_mailpro_conversation_list_head d-flex align-items-center justify-content-between gap-2">
                        <span class="o_mailpro_conversation_list_title text-truncate" t-esc="folderLabel"/>
                        <Dropdown t-if="filters.length" position="'bottom-end'"
                                  menuClass="'o_mailpro_filter_menu'">
                            <button type="button" class="o_mailpro_filter_toggle"
                                    t-att-class="{ 'o_mailpro_filter_toggle_active': state.filter }"
                                    aria-label="Filter conversations">
                                <i class="fa fa-filter" aria-hidden="true"/>
                                <span t-if="activeFilter" class="o_mailpro_filter_label"
                                      t-esc="activeFilter.name"/>
                            </button>
                            <t t-set-slot="content">
                                <div class="o_dropdown_title px-3 pb-1">Filter By</div>
                                <CheckboxItem t-foreach="filters" t-as="pill" t-key="pill.id"
                                              class="{ 'o_menu_item o_mailpro_filter_item': true, selected: state.filter === pill.id }"
                                              checked="state.filter === pill.id"
                                              closingMode="'none'"
                                              onSelected="() => this.setFilter(pill.id)">
                                    <span class="o_mailpro_filter_label" t-esc="pill.name"/>
                                    <span t-if="pill.count" class="o_mailpro_count"
                                          t-esc="folderCount(pill)"/>
                                </CheckboxItem>
                            </t>
                        </Dropdown>
                    </div>
                    <t t-if="state.loading and !state.conversations.length">
                        <div class="o_mailpro_skeleton" aria-hidden="true">
                            <div class="o_mailpro_skeleton_row" t-foreach="[1, 2, 3, 4, 5]" t-as="row" t-key="row"/>
                        </div>
                        <span class="visually-hidden" role="status">Loading conversations</span>
                    </t>
                    <t t-elif="!state.conversations.length">
                        <!-- An empty search is not an empty folder: "new
                             mail appears here" would say the mail does not
                             exist, when it may be one mailbox over. -->
                        <div t-if="state.search" class="o_mailpro_empty">
                            <h3 class="o_mailpro_empty_title">Nothing matches "<t t-esc="state.search"/>" in <t t-esc="listLabel"/>.</h3>
                            <p class="o_mailpro_empty_text">
                                The search covers this mailbox. Try another one, or clear it.
                            </p>
                        </div>
                        <div t-else="" class="o_mailpro_empty">
                            <h3 class="o_mailpro_empty_title">Nothing in <t t-esc="listLabel"/> yet.</h3>
                            <p class="o_mailpro_empty_text">
                                New mail appears here as it arrives.
                            </p>
                        </div>
                    </t>
                    <t t-else="">
                        <button t-foreach="state.conversations" t-as="conversation"
                                t-key="conversation.message_id"
                                type="button"
                                class="o_mailpro_item"
                                t-att-class="{
                                    'o_mailpro_item_active': state.selected
                                        and sameConversation(conversation, state.selected),
                                    'o_mailpro_item_unread': conversation.unread }"
                                t-att-aria-current="state.selected and sameConversation(conversation, state.selected) ? 'true' : undefined"
                                t-on-click="() => this.pick(conversation)">
                            <span t-if="conversation.unread" class="visually-hidden">Unread. </span>
                            <!--
                                The contact's own picture, the way every mail
                                client draws a list: you find the conversation
                                you want by who it is with, before you read a
                                word. A partner without a photo gets Odoo's own
                                letter circle; a sender who is nobody in the
                                database gets initials from the name on the
                                mail.
                            -->
                            <div class="d-flex align-items-start gap-2">
                                <img t-if="conversation.partner_id" class="o_mailpro_avatar flex-shrink-0"
                                     alt="" t-att-src="partnerAvatar(conversation)"/>
                                <span t-else="" class="o_mailpro_avatar o_mailpro_avatar_text flex-shrink-0"
                                      aria-hidden="true" t-esc="initials(conversation.correspondent)"/>
                                <span class="flex-grow-1 min-w-0">
                                    <span class="d-flex justify-content-between align-items-baseline gap-2">
                                        <span class="o_mailpro_from text-truncate" t-esc="conversation.correspondent"/>
                                        <span class="o_mailpro_date" t-att-title="fullDate(conversation.date)"
                                              t-esc="day(conversation.date)"/>
                                    </span>
                                    <span class="o_mailpro_subject d-block text-truncate" t-esc="conversation.subject"/>
                                    <span class="o_mailpro_preview d-block text-truncate" t-esc="conversation.preview"/>
                                    <span class="o_mailpro_meta d-flex align-items-center gap-2">
                                        <span t-if="conversation.record_name" class="o_mailpro_chip text-truncate"
                                              t-esc="conversation.record_name"/>
                                        <span class="o_mailpro_muted" t-esc="messageCount(conversation.count)"/>
                                    </span>
                                </span>
                            </div>
                        </button>
                        <div t-if="state.hasMore" class="o_mailpro_more">
                            <button type="button" class="btn btn-link btn-sm"
                                    t-on-click="() => this.loadMore()">Load more</button>
                        </div>
                    </t>
                </div>

                <t t-set="divider" t-value="'conversation_list'"/>
                <t t-call="pan_mail_pro.PaneSplitter"/>

                <!-- 3. the conversation -->
                <div t-if="showConversation" class="o_mailpro_conversation" role="region" aria-label="Conversation"
                     t-att-class="{ 'o_mailpro_conversation_composing': composer.state.open,
                                    'o_mailpro_folded': panes.folded('conversation') }"
                     t-att-inert="panes.folded('conversation')">
                    <!--
                        A new mail takes the pane the way a reply does. There
                        is no conversation behind it yet, so the head names the
                        record it is written on where a subject would go, and
                        the buttons are the same two.
                    -->
                    <t t-if="composer.state.open and composer.state.mode === 'new'">
                        <div class="o_mailpro_conversation_head">
                            <div class="d-flex align-items-start justify-content-between gap-3">
                                <t t-call="pan_mail_pro.BackToList"/>
                                <div class="min-w-0 flex-grow-1">
                                    <h2 class="o_mailpro_conversation_title">New email</h2>
                                    <div class="o_mailpro_conversation_sub">
                                        <span t-esc="state.compose and state.compose.label"/>
                                    </div>
                                </div>
                                <t t-call="pan_mail_pro.ComposerButtons"/>
                            </div>
                        </div>
                        <div class="o_mailpro_composer" role="region" aria-label="New email">
                            <ComposerForm t-props="composer.formProps"/>
                        </div>
                    </t>
                    <t t-elif="state.selected">
                        <div class="o_mailpro_conversation_head">
                            <div class="d-flex align-items-start justify-content-between gap-3">
                                <t t-call="pan_mail_pro.BackToList"/>
                                <div class="min-w-0 flex-grow-1">
                                    <h2 class="o_mailpro_conversation_title" t-esc="state.selected.subject"/>
                                    <div class="o_mailpro_conversation_sub">
                                        <span t-esc="state.selected.correspondent"/>
                                        <span t-if="state.conversation.messages.length > 1">
                                            &#183; <t t-esc="messageCount(state.conversation.messages.length)"/>
                                        </span>
                                    </div>
                                </div>
                                <!--
                                    One primary action per screen. This is it,
                                    and while a reply is open it is Send: the
                                    button does not move, it changes what it
                                    does. One action per tab, each on the tab
                                    that shows what it writes: Reply in Mail,
                                    Log note in Mail + notes. Files and
                                    Activities are lists, and neither is a
                                    place to write.
                                -->
                                <div t-if="!composer.state.open and (canReply or canLogNote or showOdooRecordButton)"
                                     class="d-flex align-items-center gap-2 flex-shrink-0">
                                    <button t-if="showOdooRecordButton" type="button"
                                            class="btn btn-secondary btn-sm o_mailpro_odoo_record_button"
                                            t-att-title="selectedRecord.name"
                                            t-on-click="() => this.showOdooRecordScreen()">
                                        <i class="fa fa-cube me-1" aria-hidden="true"/>Record
                                    </button>
                                    <button t-if="canLogNote" type="button" class="btn btn-primary btn-sm"
                                            t-on-click="() => this.logNote()">Log note</button>
                                    <button t-if="canReply" type="button" class="btn btn-primary btn-sm"
                                            t-on-click="() => this.reply()">Reply</button>
                                </div>
                                <t t-if="composer.state.open" t-call="pan_mail_pro.ComposerButtons"/>
                            </div>
                            <div t-if="state.conversation.records.length" class="o_mailpro_chips d-flex align-items-center gap-2">
                                <span class="o_mailpro_label">Linked to</span>
                                <button t-foreach="state.conversation.records" t-as="chip"
                                        t-key="chip.model + '-' + chip.res_id"
                                        type="button"
                                        class="o_mailpro_chip_button"
                                        t-on-click="() => this.openRecordChip(chip)">
                                    <!-- The app's own tile says "a quotation"
                                         faster than a word would, and the
                                         word is still there for the mouse. -->
                                    <img t-if="chip.icon" class="o_mailpro_chip_icon"
                                         t-att-src="chip.icon" t-att-alt="chip.model_label"
                                         t-att-title="chip.model_label"/>
                                    <span t-esc="chip.name"/>
                                </button>
                                <!-- Secondary, and deliberately quiet: a
                                     correctly linked conversation is the
                                     normal case and this is the exception
                                     next to it. -->
                                <button type="button"
                                        class="btn btn-link btn-sm p-0 o_mailpro_relink_toggle"
                                        t-on-click="() => this.openLinkDialog()">Change</button>
                            </div>
                            <!--
                                The suggestion is its own block, outside the
                                unlinked one, because the case it matters most
                                in is not "linked to nothing" but "linked to
                                the contact": delivered, and to a place nobody is
                                looking. A conversation with a real record never
                                carries one, because the server only records
                                a suggestion when the mail went somewhere else.

                                One suggestion, never a list. Five
                                possibilities hand the matching back to the
                                reader; one asks them to confirm or ignore,
                                which is a decision made in a second. The
                                number behind it stays in the routing log, one
                                level deeper.
                            -->
                            <div t-if="state.conversation.suggestion"
                                 class="o_mailpro_suggestion d-flex align-items-center flex-wrap gap-2">
                                <span class="o_mailpro_suggestion_text">
                                    Looks like
                                    <strong t-esc="state.conversation.suggestion.name"/>
                                </span>
                                <span class="o_mailpro_muted" t-esc="state.conversation.suggestion.reason"/>
                                <button type="button" class="btn btn-secondary btn-sm"
                                        t-on-click="() => this.acceptSuggestion()">Link it here</button>
                            </div>
                            <div t-if="!state.conversation.records.length" class="o_mailpro_unlinked">
                                <div class="d-flex align-items-center justify-content-between gap-2">
                                    <span class="o_mailpro_unlinked_title">Not linked to a record yet.</span>
                                    <button t-if="state.conversation.rejected.length"
                                            type="button" class="btn btn-link btn-sm p-0"
                                            t-att-aria-expanded="state.showRejected ? 'true' : 'false'"
                                            t-on-click="() => state.showRejected = !state.showRejected">
                                        <t t-if="state.showRejected">Hide why</t>
                                        <t t-else="">Why?</t>
                                    </button>
                                </div>
                                <ul t-if="state.showRejected" class="o_mailpro_unlinked_list">
                                    <li t-foreach="state.conversation.rejected" t-as="row" t-key="row.id">
                                        <span t-esc="row.rule_label"/>: <span t-esc="row.reason"/>
                                    </li>
                                </ul>
                            </div>
                            <!--
                                The model first, the record second, both with
                                a search box, both in one dialog. The row of
                                model chips this replaced was fine for the
                                four models a database files mail on and
                                useless for the fifth, and it left the record
                                to Odoo's own list dialog, which opens on
                                everything and knows nothing about who wrote.
                            -->
                            <div t-if="state.selected and !state.conversation.records.length"
                                 class="o_mailpro_relink_open">
                                <button type="button" class="btn btn-link btn-sm p-0 o_mailpro_relink_toggle"
                                        t-on-click="() => this.openLinkDialog()">Link to a record</button>
                            </div>
                            <!--
                                Four readings of one conversation, one strip.
                                This is what the record pane's chatter used to
                                carry: the notes, the record's own events, the
                                files and the follow-ups. A count is drawn
                                only when there is one.
                            -->
                            <div t-if="!composer.state.open"
                                 class="o_mailpro_tabs d-flex align-items-center gap-1" role="tablist"
                                 aria-label="What to read about this conversation">
                                <button t-foreach="TABS" t-as="tab" t-key="tab.id"
                                        type="button" role="tab"
                                        class="o_mailpro_tab"
                                        t-att-class="{ 'o_mailpro_tab_active': state.tab === tab.id }"
                                        t-att-aria-selected="state.tab === tab.id ? 'true' : 'false'"
                                        t-on-click="() => this.setTab(tab.id)">
                                    <span t-esc="tab.label"/>
                                    <span t-if="tabCount(tab.id)" class="o_mailpro_tab_count"
                                          t-esc="tabCount(tab.id)"/>
                                </button>
                                <!--
                                    Who Odoo notifies about this record, at the
                                    right end of the strip. Not a fifth tab: the
                                    tabs are readings of the conversation, and a
                                    tab whose whole content is three names is an
                                    empty screen with a list in the corner. The
                                    list itself is the chatter's own dropdown.
                                -->
                                <Dropdown t-if="recordThread" position="'bottom-end'"
                                          menuClass="'o-mail-Followers-dropdown ' + mailStore.discussDropdownMenuClass(this)"
                                          state="followerListDropdown">
                                    <button type="button" class="o_mailpro_followers ms-auto"
                                            t-att-class="{ 'o_mailpro_followers_following': recordThread.selfFollower }"
                                            t-att-disabled="recordThread.hasReadAccess === false"
                                            t-att-title="followersLabel">
                                        <i class="fa fa-fw" aria-hidden="true"
                                           t-att-class="recordThread.selfFollower ? 'fa-user' : 'fa-user-o'"/>
                                        <span t-esc="followersLabel"/>
                                        <span t-if="recordThread.followersCount" class="o_mailpro_tab_count"
                                              t-esc="recordThread.followersCount"/>
                                    </button>
                                    <t t-set-slot="content">
                                        <FollowerList onAddFollowers.bind="onAddFollowers"
                                                      onFollowerChanged.bind="onFollowerChanged"
                                                      thread="recordThread"
                                                      dropdown="followerListDropdown"/>
                                    </t>
                                </Dropdown>
                            </div>
                        </div>
                        <!--
                            The conversation is a stack, newest at the top: the
                            message you came for is open and the history under
                            it is one line each. A conversation of nine mails that
                            opens nine full bodies is a page you have to
                            scroll, and the newest message is the only part
                            anybody reads first.

                            The header is the toggle and it is a button, so
                            the whole history is reachable with a keyboard.
                        -->
                        <!--
                            Answering happens where the mail is. The composer
                            takes this pane instead of a dialog over the
                            screen, so the list, the record and the mail being
                            answered all stay readable while somebody writes.
                            It is Odoo's own composer form; `use_composer.js`
                            says what mounting it here costs.
                        -->
                        <div t-if="composer.state.open" class="o_mailpro_composer"
                             role="region" t-att-aria-label="composer.state.mode === 'note' ? 'Internal note' : 'Reply'">
                            <ComposerForm t-props="composer.formProps"/>
                        </div>
                        <t t-elif="state.tab === 'files'">
                            <t t-call="pan_mail_pro.ConversationFiles"/>
                        </t>
                        <t t-elif="state.tab === 'activities'">
                            <t t-call="pan_mail_pro.ConversationActivities"/>
                        </t>
                        <div t-else="" class="o_mailpro_messages">
                            <t t-foreach="state.conversation.messages" t-as="message" t-key="message.id">
                            <!--
                                A record event is one line, never a card:
                                "Stage: New to Qualified" is the whole message,
                                and forty of them as cards is a page you
                                scroll past to reach the mail.
                            -->
                            <div t-if="message.kind === 'event'" class="o_mailpro_event d-flex justify-content-between gap-2">
                                <span class="min-w-0">
                                    <t t-if="message.tracking.length">
                                        <span t-foreach="message.tracking" t-as="change" t-key="change.field"
                                              class="o_mailpro_event_change">
                                            <t t-esc="change.field"/>: <t t-esc="change.old or '-'"/>
                                            &#8594; <t t-esc="change.new or '-'"/>
                                        </span>
                                    </t>
                                    <t t-else=""><span t-esc="message.subject or message.author"/></t>
                                </span>
                                <span class="o_mailpro_date flex-shrink-0" t-att-title="fullDate(message.date)"
                                      t-esc="day(message.date)"/>
                            </div>
                            <article t-else=""
                                     class="o_mailpro_message"
                                     t-att-class="{
                                        'o_mailpro_message_open': isOpen(message),
                                        'o_mailpro_message_note': message.kind === 'note' }">
                                <button type="button" class="o_mailpro_message_head d-flex gap-2 w-100"
                                        t-att-aria-expanded="isOpen(message) ? 'true' : 'false'"
                                        t-on-click="() => this.toggleMessage(message)">
                                    <img t-if="message.author_id" class="o_mailpro_avatar flex-shrink-0"
                                         alt="" t-att-src="avatar(message)"/>
                                    <span t-else="" class="o_mailpro_avatar o_mailpro_avatar_text flex-shrink-0"
                                          aria-hidden="true" t-esc="initials(message.author)"/>
                                    <span class="o_mailpro_message_lines flex-grow-1 min-w-0">
                                        <span class="d-flex justify-content-between align-items-baseline gap-2">
                                            <span class="min-w-0 d-flex align-items-baseline gap-2">
                                                <span class="o_mailpro_message_from text-truncate"
                                                      t-att-title="message.author_email"
                                                      t-esc="message.author"/>
                                                <!-- The one thing that must never be
                                                     mistaken for a mail to the customer. -->
                                                <span t-if="message.kind === 'note'"
                                                      class="o_mailpro_note_tag flex-shrink-0">Internal note</span>
                                            </span>
                                            <span class="o_mailpro_date" t-att-title="fullDate(message.date)"
                                                  t-esc="day(message.date)"/>
                                        </span>
                                        <span t-if="!isOpen(message)"
                                              class="o_mailpro_message_snippet d-block text-truncate"
                                              t-esc="snippet(message)"/>
                                    </span>
                                </button>
                                <t t-if="isOpen(message)">
                                    <!--
                                        Who was on it, the way Outlook shows
                                        it: To and Cc on one line, the full
                                        block a click further. Its own button
                                        under the head rather than inside it,
                                        because the head is already the
                                        open/close toggle and a button in a
                                        button is not a thing. A note gets no
                                        line at all: a recipient row on a
                                        note is what makes people believe a
                                        note is an email.
                                    -->
                                    <div t-if="message.kind !== 'note'" class="o_mailpro_meta">
                                        <button type="button"
                                                class="o_mailpro_meta_toggle d-flex align-items-baseline gap-2 w-100"
                                                t-att-aria-expanded="showDetails(message) ? 'true' : 'false'"
                                                t-on-click="() => this.toggleDetails(message)">
                                            <span class="o_mailpro_meta_line flex-grow-1 min-w-0 text-truncate">
                                                <span class="o_mailpro_meta_key">To</span>
                                                <t t-esc="message.recipients or '-'"/>
                                                <t t-if="message.cc">
                                                    <span class="o_mailpro_meta_key">Cc</span>
                                                    <t t-esc="message.cc"/>
                                                </t>
                                                <!-- Which mailbox this one
                                                     came in on, once the
                                                     thread spans more than
                                                     one. On a thread that
                                                     reached a single mailbox
                                                     it is the same answer on
                                                     every line, so it is not
                                                     drawn. -->
                                                <t t-if="spansMailboxes and message.mailbox">
                                                    <span class="o_mailpro_meta_key">Mailbox</span>
                                                    <t t-esc="message.mailbox"/>
                                                </t>
                                            </span>
                                            <i class="fa fa-chevron-down o_mailpro_meta_chevron flex-shrink-0"
                                               aria-hidden="true"/>
                                        </button>
                                        <dl t-if="showDetails(message)" class="o_mailpro_meta_full">
                                            <dt>From</dt>
                                            <dd t-esc="fromLine(message)"/>
                                            <dt>To</dt>
                                            <dd t-esc="message.recipients or '-'"/>
                                            <t t-if="message.cc">
                                                <dt>Cc</dt>
                                                <dd t-esc="message.cc"/>
                                            </t>
                                            <t t-if="spansMailboxes and message.mailbox">
                                                <dt>Mailbox</dt>
                                                <dd t-esc="message.mailbox"/>
                                            </t>
                                            <dt>Date</dt>
                                            <dd t-esc="fullDate(message.date)"/>
                                        </dl>
                                    </div>
                                    <div class="o_mailpro_body" t-out="visibleBody(message)"/>
                                    <div t-if="hasQuote(message)" class="o_mailpro_quote_block">
                                        <button type="button" class="o_mailpro_quote_toggle"
                                                aria-label="Quoted history"
                                                t-att-aria-expanded="state.quotes[message.id] ? 'true' : 'false'"
                                                t-on-click="() => this.toggleQuote(message)">&#8230;</button>
                                        <div t-if="state.quotes[message.id]" class="o_mailpro_quote"
                                             t-out="quotedBody(message)"/>
                                    </div>
                                </t>
                            </article>
                            </t>
                        </div>
                    </t>
                    <div t-else="" class="o_mailpro_empty">
                        <h3 class="o_mailpro_empty_title">Pick a conversation.</h3>
                    </div>
                </div>

                <t t-set="divider" t-value="'odoo_record'"/>
                <t t-call="pan_mail_pro.PaneSplitter"/>

                <!-- 4. the record: the pane a mail client cannot have -->
                <div t-if="showOdooRecord" class="o_mailpro_odoo_record"
                     t-att-class="{ 'o_mailpro_folded': panes.folded('odoo_record') }"
                     t-att-inert="panes.folded('odoo_record')"
                     role="region" aria-label="Odoo record"
                     t-attf-style="flex-basis: {{panes.state.odoo_record}}px">
                    <OdooRecordPane t-if="selectedRecord" record="selectedRecord"
                                zoomed="panes.state.zoom"
                                zoomLabel="panes.zoomLabel()"
                                onToggleZoom.bind="() => panes.toggleZoom()"/>
                    <div t-else="" class="o_mailpro_empty">
                        <h3 class="o_mailpro_empty_title">No record yet.</h3>
                        <p class="o_mailpro_empty_text">Link this conversation and it appears here.</p>
                        <!-- Zoom hides the way back with the rest of the
                             screen, so the way back comes along. -->
                        <button t-if="panes.state.zoom" type="button"
                                class="btn btn-secondary btn-sm"
                                t-on-click="() => panes.toggleZoom()">Back to the Inbox</button>
                    </div>
                </div>

            </div>
        </div>
    </t>

    <!--
        The folders of one mailbox, `folderMailbox`. One definition, two call
        sites. A folder is only the open one when its mailbox is the open one:
        two unfolded mailboxes must not both claim to be showing Inbox.
    -->
    <t t-name="pan_mail_pro.ConversationFolders">
        <t t-set="isOpenMailbox" t-value="(folderMailbox or false) === (state.mailboxId or false)"/>
        <button t-foreach="foldersFor(folderMailbox)" t-as="folder" t-key="folder.id"
                type="button"
                class="o_mailpro_folder d-flex align-items-center justify-content-between"
                t-att-class="{ 'o_mailpro_folder_active': isOpenMailbox and folder.id === state.folder }"
                t-att-aria-current="isOpenMailbox and folder.id === state.folder ? 'true' : undefined"
                t-on-click="() => this.setFolder(folder.id, folderMailbox or null)">
            <span t-esc="folder.name"/>
            <span t-if="folder.count" class="o_mailpro_count" t-esc="folderCount(folder)"/>
        </button>
    </t>

    <!--
        The files of a conversation. Odoo's own attachment list over Odoo's
        own attachment store, so a file here behaves the way it does in the
        chatter: the same cards, the same preview on click, the same download
        and delete on hover, and the same "Attach files" underneath. A second
        implementation of an attachment is a second set of bugs.
    -->
    <t t-name="pan_mail_pro.ConversationFiles">
        <div class="o_mailpro_files">
            <AttachmentList t-if="files.length"
                            attachments="files"
                            unlinkAttachment.bind="unlinkAttachment"/>
            <div t-else="" class="o_mailpro_empty">
                <h3 class="o_mailpro_empty_title">No files on this conversation.</h3>
                <p class="o_mailpro_empty_text">Attachments on its mail and its notes appear here.</p>
            </div>
            <!--
                An upload needs a record to land on. A conversation linked to
                nothing has none, and a button that cannot say where the file
                went is worse than no button.
            -->
            <FileUploader t-if="selectedRecord" multiUpload="true"
                          onUploaded.bind="onFileUploaded">
                <t t-set-slot="toggler">
                    <button class="btn btn-link" type="button">
                        <i class="fa fa-plus-square"/>
                        Attach files
                    </button>
                </t>
            </FileUploader>
        </div>
    </t>

    <!--
        The follow-ups. `mail.activity` on the records this conversation
        touched, scheduled through Odoo's own wizard so it lands in the
        Activities clock the rest of the database reads.

        Every card is Odoo's own `mail.Activity` component, so the icon, the
        three state colours and Mark Done / Edit / Cancel are the chatter's
        and cannot drift from it. What this screen adds is the one thing the
        chatter never needs: which record the follow-up sits on, drawn only
        when the conversation touched more than one.
    -->
    <t t-name="pan_mail_pro.ConversationActivities">
        <div class="o_mailpro_activities">
            <div t-if="selectedRecord" class="o_mailpro_activities_head">
                <button type="button" class="btn btn-secondary btn-sm"
                        t-on-click="() => this.scheduleActivity()">Schedule</button>
            </div>
            <t t-if="state.conversation.activities.length">
                <t t-foreach="activities" t-as="activity" t-key="activity.id">
                    <div t-if="activityRecordName(activity)"
                         class="o_mailpro_muted text-truncate"
                         t-esc="activityRecordName(activity)"/>
                    <Activity activity="activity"
                              onActivityChanged.bind="onActivityChanged"
                              reloadParentView.bind="onActivityChanged"/>
                </t>
            </t>
            <div t-else="" class="o_mailpro_empty">
                <h3 class="o_mailpro_empty_title">Nothing scheduled.</h3>
                <p class="o_mailpro_empty_text">A follow-up with a date on it lives on the record.</p>
            </div>
        </div>
    </t>

    <!--
        One divider, three call sites. It is a separator with a value, so a
        screen reader reads a width rather than a decoration, and the arrow
        keys move it: a width has to be settable without a mouse.

        It is a width and nothing else. What shows and what folds is the row
        of buttons in the top bar, which is on screen whatever is folded, so
        a divider beside a folded pane has no width to offer and is not
        drawn at all.
    -->
    <t t-name="pan_mail_pro.PaneSplitter">
        <div t-if="panes.splitterVisible(divider)"
             t-attf-class="o_mailpro_split o_mailpro_split_{{divider}}"
             role="separator" aria-orientation="vertical" tabindex="0"
             t-att-aria-label="panes.label(divider)"
             t-att-aria-valuenow="panes.state[divider]"
             t-att-aria-valuemin="panes.panes[divider].min"
             t-att-aria-valuemax="panes.panes[divider].max"
             t-on-pointerdown="(ev) => panes.startDrag(divider, ev)"
             t-on-keydown="(ev) => panes.onKey(divider, ev)"
             t-on-dblclick="() => panes.reset(divider)"/>
    </t>

    <!--
        Discard and Send, for whichever of the three things is being written.
        One primary action per screen: while a composer is open this is it,
        and the button says what it will do: a note is logged, a reply and
        a new mail are sent.
    -->
    <!--
        The step back from a conversation to the list, on a phone, where the
        two do not fit side by side. Nothing is deselected: the list still
        marks the conversation, and a second tap opens it again.
    -->
    <t t-name="pan_mail_pro.BackToList">
        <!-- Not while writing: the composer has the screen then, and
             Discard is its way out. A back arrow that drops a draft is a
             trap. -->
        <button t-if="panes.state.small and !composer.state.open" type="button"
                class="o_mailpro_back flex-shrink-0"
                aria-label="Back to the conversation list"
                t-on-click="() => this.backToList()">
            <i class="fa fa-arrow-left" aria-hidden="true"/>
        </button>
    </t>

    <t t-name="pan_mail_pro.ComposerButtons">
        <div class="d-flex align-items-center gap-2 flex-shrink-0">
            <button type="button" class="btn btn-link btn-sm"
                    t-on-click="() => composer.close()">Discard</button>
            <button type="button" class="btn btn-primary btn-sm"
                    t-att-disabled="composer.state.sending"
                    t-on-click="() => composer.send()">
                <t t-if="composer.state.mode === 'note'">Log</t>
                <t t-else="">Send</t>
            </button>
        </div>
    </t>

    <!--
        The composer, one component deep so the env mail's composer form needs
        stops at it rather than reaching the record pane. See use_composer.js.
    -->
    <t t-name="pan_mail_pro.ComposerForm">
        <View t-props="props.viewProps"/>
    </t>

    <t t-name="pan_mail_pro.OdooRecordPane">
        <div class="o_mailpro_odoo_record_inner h-100">
            <t t-if="!state.failed">
                <!--
                    The form's own control panel is off, so the pane says which
                    record this is and offers one action: the record on the
                    whole screen. Two ways to make the record bigger side by
                    side read as one of them being broken, so there is one,
                    and it is the icon button the top bar already uses for
                    the panes. The link out of the Inbox is a step further
                    and only appears once you are on the whole screen.
                -->
                <div class="o_mailpro_odoo_record_head d-flex align-items-center justify-content-between gap-2"
                     t-on-dblclick="() => this.props.onToggleZoom()">
                    <div class="min-w-0">
                        <div class="o_mailpro_label" t-esc="props.record.model_label"/>
                        <h2 class="o_mailpro_odoo_record_name text-truncate" t-esc="props.record.name"/>
                    </div>
                    <div class="d-flex align-items-center gap-2 flex-shrink-0">
                        <!--
                            Leaving the Inbox for the record's own screen is
                            a step further, so it only appears once you are on
                            the whole screen. It wears the arrow every product
                            uses for "this opens somewhere else", because it
                            does: another URL, another breadcrumb.
                        -->
                        <a t-if="props.zoomed" href="#" class="o_mailpro_odoo_record_open"
                           t-on-click.prevent="() => this.openRecord()">
                            <span>Open in Odoo</span>
                            <i class="fa fa-external-link ms-1" aria-hidden="true"/>
                        </a>
                        <!--
                            The same round, quiet icon button as the pane
                            toggles in the top bar: it does the same kind of
                            thing to the same panes, so it looks the same.
                            The two arrows say it, in and out, the way every
                            player and every map does.
                        -->
                        <button type="button" class="o_mailpro_odoo_record_zoom"
                                t-att-aria-pressed="props.zoomed ? 'true' : 'false'"
                                t-att-aria-label="props.zoomLabel"
                                t-att-title="props.zoomLabel"
                                t-on-click="() => this.props.onToggleZoom()">
                            <i class="fa" aria-hidden="true"
                               t-att-class="props.zoomed ? 'fa-compress' : 'fa-expand'"/>
                        </button>
                    </div>
                </div>
                <View t-props="viewProps"/>
            </t>
            <div t-else="" class="o_mailpro_empty">
                <h3 class="o_mailpro_empty_title" t-esc="props.record.name"/>
                <p class="o_mailpro_empty_text">This record could not be shown here.</p>
                <button type="button" class="btn btn-secondary btn-sm"
                        t-on-click="() => this.openRecord()">Open</button>
            </div>
        </div>
    </t>

</templates>
