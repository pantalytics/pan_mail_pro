version: 2
updates:
  # Only GitHub Actions. Python dependencies are pinned by the Odoo runtime,
  # not by this repo, so dependabot has nothing useful to say about them.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    open-pull-requests-limit: 3
    commit-message:
      prefix: ci
