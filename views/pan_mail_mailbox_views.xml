<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- List View -->
    <record id="view_pan_mail_mailbox_list" model="ir.ui.view">
        <field name="name">pan.mail.mailbox.list</field>
        <field name="model">pan.mail.mailbox</field>
        <field name="arch" type="xml">
            <list string="Mailboxes" js_class="mailbox_list"
                  decoration-danger="health_status == 'error'"
                  decoration-warning="health_status == 'warning'">
                <field name="sequence" widget="handle"/>
                <field name="email" string="Mailbox"/>
                <field name="health_status" string="Status" widget="badge"
                       decoration-success="health_status == 'healthy'"
                       decoration-warning="health_status == 'warning'"
                       decoration-danger="health_status == 'error'"/>
                <!-- Editable straight from the list: which mailbox sends the
                     system email is one tick, and moving it is untick here,
                     tick there. -->
                <field name="is_notification_mailbox" string="Notification"
                       widget="boolean_toggle"/>
                <field name="mailbox_type" string="Type" optional="hide"/>
                <field name="provider" string="Provider" optional="hide"/>
                <field name="owner_user_id" string="Owner" optional="hide"/>
                <field name="sync_level" string="Sync" optional="hide"/>
                <field name="last_sync_date" string="Last Synced" optional="hide"/>
                <field name="error_message" string="Error" optional="hide"/>
            </list>
        </field>
    </record>

    <!-- Form View.

         Three speeds, three places. The identity (address, type, owner) is set
         once and read many times, so it is the title. The state (status, last
         sync, how much landed) is looked at when something is wrong, so it is
         the header and two counters, with no field to fill in. The sync
         settings are adjusted now and then, so they are the first tab; the
         things set once are the second. Nothing on the screen scrolls. -->
    <record id="view_pan_mail_mailbox_form" model="ir.ui.view">
        <field name="name">pan.mail.mailbox.form</field>
        <field name="model">pan.mail.mailbox</field>
        <field name="arch" type="xml">
            <form string="Mailbox">
                <header>
                    <!-- Every mailbox syncs something since 19.0.7.6.0: replies
                         land on their record with no setting at all, so there
                         is no configuration under which this button has
                         nothing to do. -->
                    <button name="action_sync_now" string="Sync Now"
                            type="object" class="btn-primary" icon="fa-refresh"
                            invisible="is_notification_mailbox"/>
                    <!-- Every mailbox, including the notification one: sending
                         is the one thing the consent screen does not prove. The
                         mail goes to the user who presses it, never to this
                         address. -->
                    <button name="action_test_send" string="Send Test Email"
                            type="object" class="btn-secondary" icon="fa-paper-plane"/>
                </header>
                <sheet>
                    <!-- Inherited from the one provider this database runs on;
                         only the IMAP button below reads it. -->
                    <field name="provider" invisible="1"/>
                    <field name="state" invisible="1"/>
                    <field name="active" invisible="1"/>

                    <!-- The inspection surface: is it working, and since when. -->
                    <div name="button_box" class="oe_button_box">
                        <button class="oe_stat_button" icon="fa-clock-o"
                                invisible="not last_sync_date or is_notification_mailbox">
                            <div class="o_stat_info">
                                <span class="o_stat_value">
                                    <field name="last_sync_date" readonly="1"/>
                                </span>
                                <span class="o_stat_text">Last synced</span>
                                <span class="o_stat_text" invisible="not last_sent_sync_date">
                                    Sent: <field name="last_sent_sync_date" readonly="1"/>
                                </span>
                            </div>
                        </button>
                        <button name="action_open_routing_log" type="object"
                                class="oe_stat_button" icon="fa-random"
                                invisible="is_notification_mailbox">
                            <field name="routing_log_count" widget="statinfo" string="Routed here"/>
                        </button>
                    </div>

                    <!-- The identity: read, not filled in. -->
                    <div class="oe_title">
                        <h1 class="d-flex align-items-center gap-3">
                            <field name="email" placeholder="support@company.com"
                                   class="flex-grow-1"/>
                            <field name="health_status" string="Status" widget="badge"
                                   decoration-success="health_status == 'healthy'"
                                   decoration-warning="health_status == 'warning'"
                                   decoration-danger="health_status == 'error'"
                                   readonly="1"/>
                        </h1>
                        <div class="text-muted">
                            <field name="subtitle" readonly="1" nolabel="1"/>
                        </div>
                    </div>

                    <notebook>
                        <!-- Hidden for the notification mailbox: it carries the
                             system email and syncs nothing itself. -->
                        <page string="Sync Settings" name="sync"
                              invisible="is_notification_mailbox">
                            <!-- Sending is stated, not asked. Whether this
                                 address is used is decided per message, in the
                                 composer's Send From. -->
                            <div class="text-muted mb-3">
                                Emails you send from Odoo with this mailbox picked under
                                Send From go out from this address. They show up in the
                                Sent Items of your mail app like any other mail. A note
                                sends nothing.
                            </div>

                            <!-- One ladder. Each answer keeps strictly more of
                                 the mailbox than the one above it, so the widest
                                 answer is three deliberate clicks from the
                                 default and says out loud what it does. -->
                            <span class="o_form_label">Sync level</span>
                            <field name="sync_level" widget="radio" nolabel="1" class="mt-1"/>
                            <div class="alert alert-warning mt-2 mb-0" role="alert"
                                 invisible="sync_level != 'everyone'">
                                Every sender becomes a contact in Odoo, newsletters
                                and private email included. Pick this only for an
                                address that exists to hear from strangers.
                            </div>

                            <!-- The consequence, not help text: split by the
                                 situation the reader recognises, and only the
                                 right-hand column changes with the level.
                                 Exactly one rung separates any two neighbouring
                                 answers, which is what makes it a ladder. -->
                            <table class="table table-sm o_mailpro_sync_table mt-4">
                                <thead>
                                    <tr>
                                        <th class="text-muted fw-normal">What</th>
                                        <th class="text-muted fw-normal">What happens in Odoo</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <tr>
                                        <td>A reply from your contact</td>
                                        <td>Synced</td>
                                    </tr>
                                    <tr>
                                        <td>Your reply from your mail app</td>
                                        <td>
                                            <span invisible="sync_level == 'replies'">Synced</span>
                                            <span class="text-muted" invisible="sync_level != 'replies'">Not synced</span>
                                        </td>
                                    </tr>
                                    <tr>
                                        <td>New mail from your contact</td>
                                        <td>
                                            <span invisible="sync_level not in ('contacts', 'everyone')">Synced</span>
                                            <span class="text-muted" invisible="sync_level in ('contacts', 'everyone')">Not synced</span>
                                        </td>
                                    </tr>
                                    <tr>
                                        <td>New mail from your mail app</td>
                                        <td>
                                            <span invisible="sync_level not in ('contacts', 'everyone')">Synced</span>
                                            <span class="text-muted" invisible="sync_level in ('contacts', 'everyone')">Not synced</span>
                                        </td>
                                    </tr>
                                    <tr>
                                        <td>New mail from a stranger</td>
                                        <td>
                                            <span invisible="sync_level != 'everyone'">Synced</span>
                                            <span class="text-muted" invisible="sync_level == 'everyone'">Not synced</span>
                                        </td>
                                    </tr>
                                </tbody>
                            </table>

                            <!-- Where a new conversation lands is the end of one
                                 of those lines, so it lives here and appears
                                 with the rung that lets new conversations in. -->
                            <div class="mt-3" invisible="sync_level not in ('contacts', 'everyone')">
                                <label for="route_to_team" string="Route new conversations to a team"
                                       class="o_form_label me-2"/>
                                <field name="route_to_team" widget="boolean_toggle" nolabel="1"
                                       class="d-inline-block"/>
                                <div class="text-muted mt-1">
                                    Off: email lands on the sender's contact.
                                    On: it goes to the team behind the alias you pick.
                                </div>
                                <div class="mt-2" invisible="not route_to_team">
                                    <label for="alias_id" string="Team" class="o_form_label me-2"/>
                                    <field name="alias_id" nolabel="1"
                                           required="route_to_team"
                                           options="{'no_create': True, 'no_create_edit': True}"
                                           placeholder="Select team..."
                                           class="d-inline-block"/>
                                </div>
                            </div>
                        </page>

                        <!-- The things you set once. One click away rather than
                             one screen away. -->
                        <page string="Setup" name="setup">
                            <group>
                                <group>
                                    <field name="owner_user_id" required="is_notification_mailbox"/>
                                    <!-- Derived, not asked: see _compute_mailbox_type. -->
                                    <label for="mailbox_type" string="Type"/>
                                    <div>
                                        <field name="mailbox_type" nolabel="1" class="d-inline-block"/>
                                        <div class="text-muted mt-1">
                                            Follows from the owner. Their own address is
                                            personal: only they send from it. Any other
                                            address is shared: everyone can send from it.
                                        </div>
                                    </div>
                                    <label for="is_notification_mailbox"/>
                                    <div>
                                        <field name="is_notification_mailbox" widget="boolean_toggle"
                                               nolabel="1" class="d-inline-block"/>
                                        <div class="text-muted mt-1">
                                            Odoo's own email leaves from here: invitations,
                                            password resets, reminders. Exactly one mailbox.
                                        </div>
                                    </div>
                                    <field name="sync_start_date" string="Import from"
                                           invisible="is_notification_mailbox"/>
                                </group>
                                <group>
                                    <div invisible="provider != 'imap'">
                                        <button name="action_open_account" type="object"
                                                string="Server Credentials" class="btn-link ps-0"
                                                icon="fa-key"/>
                                        <div class="text-muted mt-1">
                                            This address is reached with a server, a login and a
                                            password rather than a consent screen.
                                        </div>
                                    </div>
                                </group>
                            </group>
                        </page>
                    </notebook>

                    <!-- Error alert -->
                    <div class="alert alert-danger mt-3" role="alert" invisible="state != 'error'">
                        <strong>Sync failed:</strong> <field name="error_message" nolabel="1" readonly="1"/>
                    </div>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action -->
    <record id="action_pan_mail_mailbox" model="ir.actions.act_window">
        <field name="name">Mailboxes</field>
        <field name="res_model">pan.mail.mailbox</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Add your first mailbox
            </p>
            <p>
                A mailbox is an address Mail Pro sends and receives on, using the
                provider you set up. One of them carries the system email.
            </p>
        </field>
    </record>

    <!-- Menu: Settings → Technical → Email → Mailboxes -->
    <menuitem id="menu_pan_mail_mailbox"
              name="Mailboxes"
              parent="pan_mail_pro.menu_pan_mail_root"
              action="action_pan_mail_mailbox"
              sequence="20"
              groups="base.group_system"/>

</odoo>
